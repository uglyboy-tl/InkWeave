{
  "name": "inkweave-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "vite": "^8.0.0",
      "@vitejs/plugin-react": "^6.0.0",
      "inkjs": "^2.3.0",
      "react": "^19.0.0",
      "react-dom": "^19.0.0",
      "zustand": "^5.0.0",
      "@types/react": "^19.0.0",
      "@types/react-dom": "^19.0.0",
      "vite-plugin-dts": "^4.5.0",
      "esbuild": "^0.28.0"
    }
  },
  "scripts": {
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/dist",
    "build:core": "bun -F @inkweave/core build",
    "build:react": "bun -F @inkweave/react build",
    "build:plugins": "bun -F @inkweave/plugins build",
    "build:pkg": "bun run build:core && bun run build:react && bun run build:plugins",
    "build:web": "bun -F @inkweave/web build",
    "build:obsidian": "bun -F ink-player build",
    "build:cli": "bun -F @inkweave/cli build",
    "build": "bun run build:pkg && bun run build:web && bun run build:obsidian && bun run build:cli",
    "check": "biome check --write --error-on-warnings . && tsgo -b --noEmit",
    "test": "bun test && bun test:integration && bun test:e2e",
    "test:e2e": "playwright test",
    "test:e2e:all": "E2E_SYNTAX=1 playwright test",
    "test:integration": "bun -F '*' test:integration",
    "serve": "vite",
    "release": "bun run scripts/release.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.0",
    "@happy-dom/global-registrator": "^20.9.0",
    "@playwright/test": "^1.59.1",
    "@testing-library/jest-dom": "^6.9.0",
    "@testing-library/react": "^16.3.0",
    "@tsconfig/bun": "^1.0.10",
    "@tsconfig/vite-react": "^7.0.0",
    "@types/node": "^25.6.0",
    "@typescript/native-preview": "^7.0.0-dev.0.0",
    "bun-types": "^1.3.13",
    "typescript": "^6.0.0",
    "vite": "catalog:"
  }
}
